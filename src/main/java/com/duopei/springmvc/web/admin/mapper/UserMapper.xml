<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.duopei.springmvc.web.admin.mapper.UserMapper" >

  <select id="selectByPrimaryKey" resultType="com.duopei.springmvc.model.User" parameterType="java.lang.Integer" >
    SELECT id id,LOGIN_NAME loginName,USER_NAME userName,USER_PWD userPwd
    FROM sys_user
    where ID = #{id,jdbcType=INTEGER}
  </select>

</mapper>